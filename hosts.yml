---
# ==================================================
# SAP Ansible Lab Inventory
# Controller : ansnode (192.168.1.18)
# SAP Server : saperp (192.168.1.17)
# ==================================================

all:
  children:
    # -- SAP Application + Database Server -------------------------
    sap_servers:
      hosts:
        saperp:
          ansible_host: 192.168.1.17
          ansible_user: root
          ansible_python_interpreter: /usr/bin/python3.12

    # -- Future: Dedicated HANA DB Server --------------------------
    # hana_servers:
    #   hosts:
    #     sapha:
    #       ansible_host: 192.168.1.xx

    # -- Future: Dedicated App Server ------------------------------
    # nw_servers:
    #   hosts:
    #     sapnw:
    #       ansible_host: 192.168.1.xx
